export class Checkin {
  vehicle_id: string;
  plat_number: string;

  constructor(vehicle_id: string, plat_number: string) {
    this.vehicle_id = vehicle_id;
    this.plat_number = plat_number;
  }

}
